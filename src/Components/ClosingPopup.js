import React from 'react'

const ClosingPopup = () => {
  return (
  <div className={'closing-app open'}>
      <div style={{ textAlign: "center" }}>
        <div className="closing-text red-text text-darken-1 ">cdn stopped working, the application doesn't work, but maybe I'll find a solution... ☹</div>
      </div>
    </div>
    )
}

export default ClosingPopup